<%= provide(:title, "#{@listing.title} #{sanitize("- Edit room")}") %>

<main class="container my-3">
    <div class="header bg-white my-4 p-3 shadow-sm rounded">
        <h1 class="h2">Edit room</h1>
    </div>

    <div class="main-content bg-white p-3 shadow-sm rounded">
        <!-- render new room form -->
        <%= render 'form', room: @room, listing: @listing %>
    </div>
</main>
